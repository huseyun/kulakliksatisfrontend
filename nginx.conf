server {
    listen 80;
    server_name _;

    root /usr/share/nginx/html;
    index index.html;

    location / {
        # Sayfa yenilemelerinde 404 hatasını önleyen React Router kuralı
        try_files $uri $uri/ /index.html;
    }
}